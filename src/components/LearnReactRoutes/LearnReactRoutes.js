import React from "react";

const LearnReactRoutes = () => {
  return (
    <div className="right-container">
      <div className="landing-text">
        <h1 className="landing-para">Hello, let's get started..</h1>
        <p className="landing-para">
          This git project show cases the different usages of react routes using
          react-router-dom. Click on the links on the left menu to discover
          more.
        </p>
      </div>
    </div>
  );
};

export default LearnReactRoutes;
